project requirement :

we need to create a yt-dlp clone with a sync function that 

in this setu check if it will work also i want to add a repo type functionality where it will ask use whenevr they add a new song into thier YT playlist the it will scan the download folder if that song exsists and if does not a syn type function will download the song,
is this achivenable in the current website layout ?
